# Archivo de ejemplo de variables de entorno
# Copia este archivo a .env y configura tus valores

# Django
DEBUG=False
SECRET_KEY=tu-clave-secreta-super-segura-cambiala-en-produccion
DJANGO_SETTINGS_MODULE=config.settings.production

# Hosts permitidos (separados por coma)
ALLOWED_HOSTS=localhost,127.0.0.1,certificados.drtcpuno.gob.pe

# Base de datos
DB_NAME=certificados_db
DB_USER=certificados_user
DB_PASSWORD=tu-password-super-seguro-cambialo
DB_HOST=db
DB_PORT=5432

# Superusuario por defecto (se crea automáticamente)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@drtcpuno.gob.pe
DJANGO_SUPERUSER_PASSWORD=admin123

# Email (opcional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu-email@gmail.com
EMAIL_HOST_PASSWORD=tu-password-de-app

# Seguridad (producción)
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
